yt-dlp>=2024.1.0
opencv-python>=4.9.0
numpy>=1.26
insightface>=0.7.3
onnxruntime>=1.17.0
scikit-learn>=1.4.0
pandas>=2.2.0
tqdm>=4.66.0
supervision>=0.26.0
trackers>=2.0.0
pytest>=8.0.0

# Milestone 2: OCR, captioning, search UI.
# NOTE: pytesseract needs the Tesseract binary (macOS: `brew install tesseract`).
pytesseract>=0.3.10
transformers>=4.40.0
torch>=2.2.0
pillow>=10.0.0
streamlit>=1.33.0
sentence-transformers>=2.6.0

# Milestone 3: story / summary / timeline via OpenRouter (Gemma 4).
# The OpenRouter endpoint is OpenAI-compatible, so plain HTTP is enough — no SDK.
# NOTE: generating needs OPENROUTER_API_KEY; replaying data/llm_cache/ does not.
requests>=2.31.0

# Milestone 4: speech transcription (optional audio channel).
# NOTE: transcribe.py also needs the ffmpeg binary (macOS: `brew install ffmpeg`).
# Absent this package the transcription stage skips gracefully.
faster-whisper>=1.0.0

# Optional: face super-resolution (config.SR_ENABLE). dnn_superres lives in the
# contrib build, which CONFLICTS with opencv-python — install only if using SR,
# and provide the model file at config.SR_MODEL_PATH:
#   pip uninstall opencv-python && pip install opencv-contrib-python
